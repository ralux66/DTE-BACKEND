const { BulkCreate, createOne, find, findOrCreate, list,create,submitBill } = require('./bill.controller');

module.exports = {
    BulkCreate,
    createOne,
    find,
    findOrCreate,
    list,
    create,
    submitBill
}